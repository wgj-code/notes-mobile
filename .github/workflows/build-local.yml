# B-015: Mobile 本地构建 CI/CD
# 不依赖 EAS，使用 GitHub Actions runner 本地构建 APK
# 增量策略：缓存 node_modules + .gradle + build 目录

name: Build Android APK (Local)

on:
  push:
    branches: [main]
    paths:
      - 'src/**'
      - 'android/**'
      - 'scripts/**'
      - 'app.json'
      - 'package.json'
      - 'package-lock.json'
  workflow_dispatch:

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm install --legacy-peer-deps
      - run: npx jest --forceExit --detectOpenHandles --testPathIgnorePatterns='offline\.test|sync\.test'

  build:
    name: Build APK
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - uses: android-actions/setup-android@v3

      # 增量策略：缓存 Gradle 构建目录（不缓存 node_modules，确保 postinstall 补丁执行）
      - uses: actions/cache@v4
        with:
          path: |
            android/.gradle
            android/app/build
          key: android-gradle-${{ runner.os }}-${{ hashFiles('package-lock.json') }}
          restore-keys: |
            android-gradle-${{ runner.os }}-

      - run: npm install --legacy-peer-deps

      # 加载环境变量
      - name: Load .env
        run: |
          if [ -f .env ]; then
            set -a; source .env; set +a
            echo "Loaded $(grep -c EXPO_PUBLIC .env) EXPO_PUBLIC_* vars"
          fi

      # 复用现有构建脚本（增量构建）
      - name: Build debug APK (slim)
        run: bash scripts/build-debug-apk.sh --slim

      # 上传 APK 产物
      - uses: actions/upload-artifact@v4
        with:
          name: debug-apk
          path: build-*.apk
          retention-days: 30
